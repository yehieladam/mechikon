# deps / build
node_modules/
dist/
build/
*.tsbuildinfo

# the ~185MB model is fetched at runtime & cached by the browser — never commit it
*.onnx
models/

# env / secrets
.env
.env.*

# OS / editor
.DS_Store
Thumbs.db
*.log
.vscode/
.idea/

# scratch
scratchpad/
tmp/

dist-web/

_*.mjs

# Built PDF test fixtures (regenerated by build-pdf-fixtures)
web/test-fixtures/*.pdf

# Playwright
test-results/
playwright-report/

# Local AI assistant config — kept out of the public repo
CLAUDE.md

# Vendored OCR runtime + traineddata (fetched at build/setup, not committed — like the NER model)
web/public/vendor/
scripts/*.jsonl
scripts/*.log
scripts/ocr-calibration*.json

# Claude Code local session data
.claude/
